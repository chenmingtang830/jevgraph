[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "jevgraph"
version = "0.2.0"
description = "Evidence-backed knowledge graph construction with typed relation decisions."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "Richard Tang" }]
keywords = ["knowledge-graph", "relation-extraction", "jev", "information-extraction"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "httpx>=0.27,<1",
  "PyYAML>=6,<7",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "pytest-cov>=5,<7",
  "ruff>=0.9,<1",
]

[project.scripts]
jevgraph = "jevgraph.cli:main"

[project.urls]
Homepage = "https://github.com/chenmingtang830/jevgraph"
Repository = "https://github.com/chenmingtang830/jevgraph"
Issues = "https://github.com/chenmingtang830/jevgraph/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/jevgraph"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
